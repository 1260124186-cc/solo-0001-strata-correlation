# 地层剖面编录与对比服务

面向地质编录人员的 Go HTTP 后端，以剖面、分层、锁定版本和对比结果为核心。服务仅处理岩层深度与岩性，不依赖在线地理服务。

## 实体和规则
剖面包含名称、地点描述、总深度（整数毫米）、分层数组、版本和状态。分层包含顶底深度、岩性、描述、可选标志层。支持砂岩、泥岩、灰岩、页岩、砾岩和未知岩性。深度上限一千米，每个剖面最多 500 层；文字字段有明确长度限制。
草拟剖面允许空隙，但不允许重叠和越界；锁定前必须完整覆盖零深度到总深度。锁定后不能修改，重新打开会创建下一版本，历史版本仍然可读。
每次写入使用预期版本；同一版本的并发请求仅一个成功。片段式分层替换整体提交，校验失败不改变任何层。剖面每个版本和事件作为一个持久化单元原子写入磁盘。单进程独占数据目录，异常退出由操作系统释放锁。

## 测线
为把多份剖面按现场测线顺序保存，测线由若干位置组成：每个位置手工填写沿线整数毫米距离，并绑定一个已锁定的具体剖面版本。位置顺序由规范键 `(距离, 并列序号, 剖面编号)` 唯一决定，不依赖内部存储顺序；同距离允许不同剖面并列，同一剖面重复出现则拒绝。引用的版本缺失或未锁定都拒绝写入和定稿。测线为草稿时可改元数据、整体替换位置、调整排序；排序只能在距离单调不减前提下消解并列，缺漏或重复的顺序列表拒绝。定稿要求至少两个位置，定稿后顺序与每个引用版本一并冻结，不可编辑、重排或重开。成员剖面产生新版本或重新打开不替换冻结引用，只在视图中给出 outdated 等更新提示；需要新版本时由用户显式 fork 出一条新草稿并自行替换引用，旧定稿仍按当时内容查询。不接入地图或在线坐标服务。

## 工作流
1. 编录：创建剖面、一次性替换分层、读取深度覆盖缺口、修正分层。
2. 锁定与修订：检查连续性、锁定版本、读取历史、重新打开并修订。锁定失败保留原状态。
3. 对比：选定两个已经锁定的历史版本，指定右侧深度偏移，按双方边界切分共同区间，计算岩性相同长度和比例，保存不可变结果。相同输入复用同一结果。
对比辅助接口 POST /api/v1/comparison-offsets 使用共同标志层所需偏移的中位数提供建议，并输出残差，用户显式采纳后才生成结果。
4. 查阅：按岩性、地点、名称和状态筛选剖面，分页读取历史事件与分层；读取对比结果的 JSON 或 CSV。GET /profiles/{id}/at 可查询当前或历史版本中的某一深度；GET /profiles/{id}/diff 按区间和元数据字段展示历史差异。
5. 测线：创建草稿、绑定锁定版本与沿线距离、整体替换位置、调整同距离并列先后；定稿冻结顺序与引用；定稿后仅提示成员剖面更新，需新版本时 fork 新草稿，旧定稿保持可查。

## 模块与接口
cmd/stratad 提供启动入口。internal/api 处理 HTTP；internal/catalog 编排剖面与测线工作流；internal/geology 实现深度、状态和验证；internal/line 实现测线位置、距离并列与规范顺序；internal/correlation 计算共同区间；internal/persistence 负责快照、锁和数据恢复；internal/config 解析环境变量和启动参数。完整接口见 README。

## 验证计划
默认 testing=deferred，不生成测试文件和测试数据。后续专项测试任务添加单元测试和浏览器无关的 API 回归测试。当前使用临时数据目录启动真实 HTTP 服务，执行编录、锁定修订、对比和查阅生产冒烟流程，并检查失败边界和重启恢复。所有检查有超时且不访问外网。

## 容量与运行边界
最多 2000 个剖面、每个剖面 500 个版本、10000 个对比结果、1000 条测线（每条最多 500 个位置，沿线距离上限 100 千米），持久化快照最多 64 MiB。当前基线适用于 Linux/macOS 单进程本机服务，无外部依赖、无前端、无数据删除、无地图或在线坐标服务和多节点复制。正式测试套件由后续专项测试任务添加。
