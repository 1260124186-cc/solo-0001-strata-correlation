# 地层剖面编录与对比服务

面向地质编录人员的 Go HTTP 后端，以剖面、分层、锁定版本和对比结果为核心。服务仅处理岩层深度与岩性，不依赖在线地理服务。

## 实体和规则
剖面包含名称、地点描述、总深度（整数毫米）、分层数组、版本和状态。分层包含顶底深度、岩性、描述、可选标志层。支持砂岩、泥岩、灰岩、页岩、砾岩和未知岩性。深度上限一千米，每个剖面最多 500 层；文字字段有明确长度限制。
草拟剖面允许空隙，但不允许重叠和越界；锁定前必须完整覆盖零深度到总深度。锁定后不能修改，重新打开会创建下一版本，历史版本仍然可读。
同一剖面支持从任意历史版本分叉出并行修订线：分叉固定来源版本（fork_point）并产生新版本与 `br_` 线编号，事件记录分叉点；主线恒为 `main`。各修订线独立编录、锁定和重新打开，写入携带 `branch` 与预期头版本，互不影响。版本号在剖面内全局单调分配，不再由历史位置推断；历史读取、差异比较、锁定和重新打开一律按事件中的真实版本号工作。提供分叉线列表与分叉线/主线关系查询（共同基点、领先/落后、是否已合并）。分叉线可经三向合并预览逐项差异与冲突后合并回主线：合并在主线产生新版本，事件记录来源线、来源版本和基点，不覆盖任何已有版本，被合并来源版本保持可读。
每次写入使用预期版本；同一修订线头版本的并发请求仅一个成功。片段式分层替换整体提交，校验失败不改变任何层。剖面每个版本和事件作为一个持久化单元原子写入磁盘。单进程独占数据目录，异常退出由操作系统释放锁。

## 工作流
1. 编录：创建剖面、一次性替换分层、读取深度覆盖缺口、修正分层。
2. 锁定与修订：检查连续性、锁定版本、读取历史、重新打开并修订。锁定失败保留原状态。
2.1 并行修订：从任意历史版本分叉，两条线各自编录与锁定；查询分叉线关系；合并前预览逐项差异与冲突，无冲突时合并产生新主线版本，来源版本保持可读。
3. 对比：选定两个已经锁定的历史版本，指定右侧深度偏移，按双方边界切分共同区间，计算岩性相同长度和比例，保存不可变结果。相同输入复用同一结果。
对比辅助接口 POST /api/v1/comparison-offsets 使用共同标志层所需偏移的中位数提供建议，并输出残差，用户显式采纳后才生成结果。
4. 查阅：按岩性、地点、名称和状态筛选剖面，分页读取历史事件与分层；读取对比结果的 JSON 或 CSV。GET /profiles/{id}/at 可查询当前或历史版本中的某一深度；GET /profiles/{id}/diff 按区间和元数据字段展示历史差异。

## 模块与接口
cmd/stratad 提供启动入口。internal/api 处理 HTTP；internal/catalog 编排剖面工作流；internal/geology 实现深度、状态和验证；internal/correlation 计算共同区间；internal/persistence 负责快照、锁和数据恢复；internal/config 解析环境变量和启动参数。完整接口见 README。

## 验证计划
默认 testing=deferred，不生成测试文件和测试数据。后续专项测试任务添加单元测试和浏览器无关的 API 回归测试。当前使用临时数据目录启动真实 HTTP 服务，执行编录、锁定修订、对比和查阅生产冒烟流程，并检查失败边界和重启恢复。所有检查有超时且不访问外网。

## 容量与运行边界
最多 2000 个剖面、每个剖面 500 个版本、10000 个对比结果，持久化快照最多 64 MiB。当前基线适用于 Linux/macOS 单进程本机服务，无外部依赖、无前端、无数据删除和多节点复制。正式测试套件由后续专项测试任务添加。
