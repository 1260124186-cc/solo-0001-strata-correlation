# 地层剖面编录与对比服务

面向地质编录人员的 Go HTTP 后端，以剖面、分层、锁定版本和对比结果为核心。服务仅处理岩层深度与岩性，不依赖在线地理服务。

## 实体和规则
剖面包含名称、地点描述、总深度（整数毫米）、分层数组、版本和状态。分层包含顶底深度、岩性、描述、可选标志层。支持砂岩、泥岩、灰岩、页岩、砾岩和未知岩性。深度上限一千米，每个剖面最多 500 层；文字字段有明确长度限制。
草拟剖面允许空隙，但不允许重叠和越界；锁定前必须完整覆盖零深度到总深度。锁定后不能修改，重新打开会创建下一版本，历史版本仍然可读。
每次写入使用预期版本；同一版本的并发请求仅一个成功。片段式分层替换整体提交，校验失败不改变任何层。剖面每个版本和事件作为一个持久化单元原子写入磁盘。单进程独占数据目录，异常退出由操作系统释放锁。

## 工作流
1. 编录：创建剖面、一次性替换分层、读取深度覆盖缺口、修正分层。
2. 锁定与修订：检查连续性、锁定版本、读取历史、重新打开并修订。锁定失败保留原状态。
3. 对比：选定两个已经锁定的历史版本，指定右侧深度偏移，按双方边界切分共同区间，计算岩性相同长度和比例，保存结果。结果携带输入派生编号和覆盖完整负载的内容指纹；相同输入复用同一结果实例，删除后以相同输入重算则编号相同而指纹不同。对比结果可显式删除。
对比辅助接口 POST /api/v1/comparison-offsets 使用共同标志层所需偏移的中位数提供建议，并输出残差，用户显式采纳后才生成结果。
4. 复核：针对已保存的对比结果追加结论（复核人、结论、疑点区间、采用状态），固化当时的结果内容指纹与左右两侧版本。结论分草拟、已确认、已被替代三种状态：已确认结论不可覆盖，只能追加带替代理由的替代记录，确认替代时原子翻转原结论，替代链双向可查。每条结论按预期序号乐观追加，并发仅一个成功，失败响应附带最新结论顺序。结果绑定内容指纹而非编号：相同输入删除后重算产生新实例，旧复核明确标注针对的实例且不会自动挂接，必须人工迁移并在两侧保留迁移链接。对比结果删除后复核仍可读，并标记依据的结果已不存在。
5. 查阅：按岩性、地点、名称和状态筛选剖面，分页读取历史事件与分层；读取对比结果的 JSON 或 CSV。GET /profiles/{id}/at 可查询当前或历史版本中的某一深度；GET /profiles/{id}/diff 按区间和元数据字段展示历史差异。

## 模块与接口
cmd/stratad 提供启动入口。internal/api 处理 HTTP；internal/catalog 编排剖面工作流；internal/geology 实现深度、状态和验证；internal/correlation 计算共同区间；internal/review 保存复核结论、替代链与人工迁移；internal/persistence 负责快照、锁和数据恢复；internal/config 解析环境变量和启动参数。完整接口见 README。

## 验证计划
默认 testing=deferred，不生成测试文件和测试数据。后续专项测试任务添加单元测试和浏览器无关的 API 回归测试。当前使用临时数据目录启动真实 HTTP 服务，执行编录、锁定修订、对比和查阅生产冒烟流程，并检查失败边界和重启恢复。所有检查有超时且不访问外网。

## 容量与运行边界
最多 2000 个剖面、每个剖面 500 个版本、10000 个对比结果（删除后可按相同输入重新生成）、10000 个复核线程且每个线程最多 1000 条结论，持久化快照最多 64 MiB。当前基线适用于 Linux/macOS 单进程本机服务，无外部依赖、无前端、无剖面/版本删除和多节点复制；对比结果删除后其复核记录继续保留。正式测试套件由后续专项测试任务添加。
